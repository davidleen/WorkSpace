
buildscript {
    repositories {
        jcenter()
        mavenCentral()

    }
    dependencies {

    }
}

allprojects {
    repositories {
        jcenter()
        maven{ url LOCAL_MAVEN_URL}
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
